@using LeagueApi.Models
@model LeagueApi.Models.TeamList


    @for (var i = 0; i < Model.Teams.Count; i++)
    {
        @Html.DisplayFor(m => m.Teams[i],  new { ChampionData = (ChampionsResponse) ViewBag.ChampionData, Index = i })
    }